apiVersion: v1
kind: Service
metadata:
  name: apigo
  namespace: kafka
spec:
  selector:
    app: golang
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
  type: ClusterIP